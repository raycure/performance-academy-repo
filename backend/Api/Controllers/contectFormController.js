import Users from '../Models/userModel.js';
import * as dotenv from 'dotenv';
dotenv.config();
const contectFormController = async (req, res) => {
	try {
		const contectFormData = {};
	} catch (error) {}
};

export default contectFormController;
